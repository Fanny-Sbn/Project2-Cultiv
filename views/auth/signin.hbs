<h1>Welcome Back !</h1>

{{#if msg }}
<p class="message {{msg.status}}">{{msg.text}}</p>
{{/if}}
<form method="post" action="/auth/signin" class="form">
    <label for="email">Email</label>
    <input class="input" name="email" type="text" value="test@cultiv.com">

    <label for="password">Password</label>
    <input class="input" name="password" type="password" value="1234">

    <button class="btn">Enter</button>
    <hr>
    <p>Don't have an account yet ? <a href="/auth/signup" class="link">Sign Up</a></p>
</form>